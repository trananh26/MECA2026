<UserControl x:Class="SWM.UI.View.uc_Buffer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SWM.UI.View"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="240">
    <Border BorderBrush="{StaticResource BufferEmptyBorderBrush}" BorderThickness="1" CornerRadius="4" SnapsToDevicePixels="True">
    <Grid x:Name="rootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="1.2*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" x:Name="headerPanel">
            <TextBlock x:Name="rtgSlottray" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16" FontWeight="DemiBold"
                        Text="{Binding Port_Name,Mode=OneWay,
                        RelativeSource={RelativeSource AncestorType=UserControl}}" 
                        Foreground="{Binding colorText,RelativeSource={RelativeSource AncestorType=UserControl},UpdateSourceTrigger=PropertyChanged}"/>
            <Rectangle x:Name="headerDivider" StrokeThickness="1" Height="1" VerticalAlignment="Bottom" Stroke="{StaticResource BorderMediumBrush}"/>
        </Grid>

        <Grid Grid.Row="1" x:Name="detailRow1">
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{StaticResource BufferDetailTextBrush}" FontSize="14"
                            Text="{Binding Material_Code,Mode=OneWay,
                        RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        </Grid>

        <Grid Grid.Row="2" x:Name="detailRow2">

            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="14" Foreground="{StaticResource BufferDetailTextBrush}"
                            Text="{Binding Material_ID,Mode=OneWay,
                        RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        </Grid>

        <Grid Grid.Row="3" x:Name="detailRow3">
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="14" Foreground="{StaticResource BufferDetailTextBrush}"
                            Text="{Binding Aging_Time,Mode=OneWay,
                        RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        </Grid>

    </Grid>
    </Border>
</UserControl>
